# Tests

Frumpy keeps its Fortran test executables in `test/` and Python regression
fixtures in `python/tests/`.

This directory exists so the Spec Kitty software-dev path convention has a
tracked `tests/` root without moving the project's real test layout.
